{
  "endpoint": "PASTE_YOUR_APPS_SCRIPT_EXEC_URL_HERE",
  "cohortName": "LD810",
  "cohortLabel": "LD 810",
  "_note": "Copy to config.json and fill in. endpoint must end in /exec, never /dev. Class practice is unlocked by email against the roster, not a password - see grantAccess()/grantMany() in Code.gs."
}
