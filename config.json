{
  "endpoint": "PASTE_THE_EXEC_URL_HERE",
  "_note": "Apps Script Web app URL. Must end in /exec, never /dev. Get it from Deploy > Manage deployments in the Apps Script editor. This is the only file you edit when the URL changes."
}
